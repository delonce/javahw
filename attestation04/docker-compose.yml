services:
  postgres:
    image: postgres:latest
    container_name: postgres
    env_file: db.env
    ports:
      - "5432:5432"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    env_file: db.env
    depends_on:
      - postgres
    ports:
      - "9999:9999"
    command:
      - java -jar app.jar